instalando e simulado uma variavel de ambiente 

buildar:
` go build -o garrafa main.go`

🔧 Como usar

Dê permissão:

`chmod +x install.sh`

Execute:

`./install.sh`

commandos do sh:
    `sudo mv seu_binario /usr/local/bin/
sudo chmod +x /usr/local/bin/seu_binario`